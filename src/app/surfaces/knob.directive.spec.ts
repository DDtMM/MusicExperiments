import { KnobDirective } from './knob.directive';

describe('KnobDirective', () => {
  it('should create an instance', () => {
    const directive = new KnobDirective();
    expect(directive).toBeTruthy();
  });
});
