import { PressEventsDirective } from './press-events.directive';

describe('PressEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new PressEventsDirective();
    expect(directive).toBeTruthy();
  });
});
